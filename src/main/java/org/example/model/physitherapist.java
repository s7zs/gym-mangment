package org.example.model;

public class physitherapist extends users{
    public physitherapist(Roles role, String password, String username) {
        super(Roles.physiotherapist, password, username);
    }
}
